sonar.projectKey=CyrilB1531_aj_sr04m
sonar.organization=cyrilb1531

# This is the name and version displayed in the SonarCloud UI.
sonar.projectName=aj_sr04m

# Scan only the component sources and public headers; skip examples and
# tests/ (they are built for compile_commands.json but should not count
# against coverage / quality gates).
sonar.sources=src,include
sonar.tests=tests/cases

sonar.sourceEncoding=UTF-8
